# Database Configuration (Optional)
# If DATABASE_URL is not provided, the app will use in-memory storage with sample data
# DATABASE_URL=postgresql://username:password@localhost:5432/egglife

# Server Configuration
NODE_ENV=development
PORT=5000